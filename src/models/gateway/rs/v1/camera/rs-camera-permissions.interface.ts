export interface RsCameraPermissions {
  live?: boolean;
  recorded?: boolean;
  exportable?: boolean;
  allow_ptz?: boolean;
  allow_snapshot?: boolean;
  allow_light_control?: boolean;
  audio?: boolean;
  quickTrackCamera?: boolean;
}
